import React from "react";
import Tilt from "react-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className="xs:w-[250px] w-full">
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className="w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card"
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className="bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col"
      >
        <img
          src={icon}
          alt="web-development"
          className="w-16 h-16 object-contain"
        />

        <h3 className="text-white text-[20px] font-bold text-center">
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className="mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]"
      >
        Hi there, I'm Abhinaba Biswas, an enthusiastic Machine Learning
        Developer with a passion for leveraging data and algorithms to create
        intelligent systems. With a background in AI and ML, I've honed my
        skills in developing, deploying, and optimizing ML models to solve
        complex problems. I'm currently focused on building scalable ML models
        for natural language processing, experimenting with deep learning
        frameworks like TensorFlow, and implementing ML solutions in cloud
        environments such as AWS, GCP, or Azure. I enjoy exploring new
        technologies and staying updated with the latest advancements in AI and
        ML. I'm looking to collaborate on open source AI/ML projects, research
        and development in emerging areas of machine learning, and educational
        content creation. My skills include Python, C, and C++ programming, as
        well as expertise in TensorFlow, Keras, scikit-learn, Pandas, and NumPy.
      </motion.p>

      <div className="mt-20 flex flex-wrap gap-10">
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
